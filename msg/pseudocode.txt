int main (int argc, char *argv[]) {
    Проверяем аргументы командной строкиб если количество не равно 2, то выводим ошибку.
    
    Преобразуем 2 аргумент массива в число при помощи strtol. Функция преобразования вроде есть в одном из уроков.
    Проверяем на корректный ввод 2 аргумента. int ch_quant = strtol ();

    Создаем очередь сообщений, проверяем на корректное создание. Записываем значение в int msgid = msgget (IPC_PRIVATE, IPC_CREAT | IPC_EXCL | 0666);

    При помощи fork () вызываем нужное количество дочерних процессов. Проверяем на корректное создание дочерних процессов.

    Программа делится на две части, если это родитель, то:
        Мы создаем цикл с количеством итераций равным ch_quant. Для каждой итерации мы записываем в буфер номер итерации и
        отправляем сообщение. 
        Родитель считывает сообщение отправленное ребенком и 
    Если это ребенок:
        Ребенок получает сообщение msgrcv и печатает в командную строку. Потом ребенок отправляет сообщение об успешной 
        обработке сообщения (пустое сообщение) и завершает работу (?).

}